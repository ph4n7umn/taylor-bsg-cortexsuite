Stuff TODO

Ask chetan about where timing analysis should be on motion estimation
Fix the Readme for motion estimation
Make the "includes" file "global" so we only have one include file instead of a ton of them
See if it is useful to make ME and SRR access the same set of files
In PART2 folder there is a new cody of LDA and MmotionEstimation. I have uploaded the old ME version, try and find out why we have a new one (think it was dataset size?) (Will be using the NEW lda implentation)
Wonder if we need different directories for the datasets that we have?
What kind of optimization should we use? Some of them originally had -03 but in sd-vbs we used -02 option so which to use?
Perhaps change the name of functions and variables in some of the bechmarks, LDA has some dumb names for stuff. PCA has variables names that are just single lettter.
Find out why run-large for LDA and stuff take pretty much no time to complete..?
Add information that LDA only benchmarking "REF"
Add information that PCA only running CORL Matrix
Rename App.c files in SRR and Motion Estimation
In Makefiles -02 -lm is redundant because C_COMPILE has these and C_FLAGS does as well
Add LIBLINEAR to the bitbuck
Make sure the "CYCLES ELAPSED number" makes sense for the benchmarks
Structure the folders in a nice way
Add COMPILE_G,COMPILE_PG flag to makefile
Ponder if we can make a global makefile.common that kind of does everything(prob kind of hard to do)
For results, perhaps a results folder similar to the way LDA does it?
Modify the rest of the benchmarks to use the /common/ directory makefile
    and add the .phony flag simliar to LDA
IMPORTANT: LDA MAKEFILE IS USING LOCAL DIRECTORY STRUCTURE NEED TO CHANGE
LDA still broken on small datasets
Sphinx/SVD makefile is not in line with the other makefiles. Includes the name of the binary (a.out)
Double check sphinx medium/large datasets
Sphinx naming is an issue because there is a folder called "INCLUDES" and my timing is called "INCLUDE"
README for Sphinx and SVD
Figure out what to do about RESULTS when running Liblinear, right now the  predict is removed and we're only benchmarking train
